# 开发部署范本：公众号排版网页版（gzh-design-web）

> 本文把「gzh-design-web」项目从需求 → 设计 → 开发 → 部署的完整链路整理成范本，
> 以后做类似「**skill / 脚本 → 网页工具**」的项目可直接套用。
> 仓库：https://github.com/ideajoker/gzh-design-web （公开，AGPL-3.0）

---

## 一、项目定位

把一个「本地运行的 AI skill / Python 脚本」改造成**普通用户可用的网页工具**：

- 用户粘贴 / 上传文章 → 选主题 → 一键调用 LLM 排版 → 预览 → 复制粘贴到目标平台（这里是微信公众号后台）。
- 不要求用户懂 Markdown、不懂技术也能用。
- 模型可插拔（DeepSeek、Kimi、任意 OpenAI 兼容端点）。

**可复用本质**：`本地 skill/脚本 + 浏览器 UI + Serverless 代理 + 一键部署`。

---

## 二、核心需求清单（P0）

| 模块 | 需求 | 是否交付 |
|------|------|----------|
| 输入 | 支持 Markdown / 纯文本；支持 **Word(.docx) 上传**；支持 **Word 带格式粘贴** | ✅ |
| 主题 | **6 套内置主题**快捷选择 + **自定义主题生成** | ✅ |
| 模型 | 默认 DeepSeek + Kimi；允许添加多个自定义 OpenAI 兼容模型；**字段顺序固定**：显示名称(可选) → API 地址 → API KEY → 模型名称 | ✅ |
| 历史 | 排版历史：查看 / 复制 / 删除 / 批量删除（本地） | ✅ |
| 图床 | imgbb 上传，用户自填 Key（BYOK） | ✅ |
| 预览 | 手机预览 + 一键复制富文本（直接粘进公众号后台保样式） | ✅ |
| 编辑 | 左富文本（带格式粘贴）→ 中转 Markdown → 右渲染预览 三栏 | ✅ |

---

## 三、技术选型与理由

| 层 | 选型 | 理由 |
|----|------|------|
| 前端框架 | **Vite + React + TypeScript** | 快、轻、生态全 |
| UI 组件库 | **Semi Design (@douyinfe/semi-ui)** | 用户指定；API 友好。**注意装的是 2.101.1，没有 Drawer 组件**，侧边面板一律用 Modal 实现 |
| 后端 | **Cloudflare Pages Functions**（TS Worker） | 无需自己维护服务器；与前端同源部署；CI 自动发布 |
| 存储 | **浏览器 localStorage** | 用户小白、不做登录；密钥只存本地，不落服务端 |
| 密钥模式 | **BYOK（Bring Your Own Key）** | 用户自己填模型 Key / 图床 Key；服务端只做代理转发，绝不持久化 |
| 部署 | **GitHub Actions + Cloudflare Pages** | push 即自动构建发布；凭证只在仓库 Actions secrets，开发者不经手 |
| 许可 | **AGPL-3.0** | SaaS / 公开网页服务必须公开源码；用公开仓库 + 页脚「源码」链接满足义务 |

---

## 四、架构总览

```
浏览器（React 前端）
   │  文章 / 主题 / 模型 Key（localStorage）
   ▼
Cloudflare Pages（静态前端 dist + Pages Functions）
   ├─ /api/layout   拼 SKILL 提示词 → 调 LLM → 强制合规校验 → 返回公众号 HTML
   ├─ /api/themes   返回内置主题列表
   ├─ /api/upload   代理上传图片到 imgbb（BYOK）
   ├─ /api/theme    自定义主题：调 LLM 生成区块库
   └─ /api/generate 根据提示词让 LLM 生成文案
        │
        ▼
   LLM 服务商 / imgbb（经 Worker 代理，规避 CORS、保护 Key）
```

**为什么需要 Worker 代理**：浏览器直连第三方 API 会有 CORS 问题，且 Key 会暴露在前端。
Worker 在服务端转发，Key 来自用户 localStorage、随请求带上、不落库。

---

## 五、关键设计决策（可复用套路）

1. **skill 资产打包进仓库**
   - Cloudflare Workers 不能跑 Python，所以把 skill 的 `SKILL.md` + `references/*.md` 用
     `scripts/build-assets.mjs` 打包成 `worker-lib/skillAssets.ts` 提交进仓库。
   - **CI 绝不重新跑打包**（构建机没有本地 skill，会清空产物）；只用已提交的 `skillAssets.ts`。

2. **Python 脚本移植为 TS**
   - `validate_gzh_html.py`、`extract_docx.py`、`component_lint.py` 移植为
     `worker-lib/{validate,extractDocx,componentLint}.ts`（Cloudflare 只跑 JS/TS）。
   - docx 解析用 `jszip` 在浏览器/Worker 侧做。

3. **富文本 → Markdown → 预览 三栏流程**
   - 左：`contentEditable` 富文本编辑器，拦截 `paste` 事件，用 `DOMParser` 清洗 Word HTML
     （只保留 p/b/i/u/s/h1-6/ul/ol/li/blockquote/a/img，去掉 style/class）。
   - 中：点「转换为 Markdown」→ 手写轻量 `htmlToMarkdown` 转换器（见第六节踩坑）→ 可二次编辑。
   - 右：调用 `/api/layout` 渲染公众号 HTML 预览。

4. **合规校验（公众号约束）**
   - 公众号后台不接受 `<style>/<script>/<div>/class/id/position:fixed/...`，
     所有样式必须 inline。后端 `validate` 强制校验，失败时自动纠正一次重试。

5. **AGPL 合规**
   - 仓库必须公开；`src/config.ts` 的 `REPO_URL` 指向仓库，页脚放「源码」链接。

---

## 六、踩坑与解决方案（重点，复用价值最高）

| 坑 | 现象 | 解决 |
|----|------|------|
| 1. Cloudflare 不自动建项目 | `wrangler pages deploy --project-name=xxx` 报 `Project not found [8000007]` | 部署前先建项目：用 Cloudflare REST API `curl POST /accounts/{id}/pages/projects`（已存在返回 409 可忽略，`|| true` 容错） |
| 2. wrangler 参数不兼容 | `wrangler pages project create xxx --yes` 报 `Unknown argument: yes`（3.114.17） | 不直接用 wrangler 建项目，改用上面的 REST API |
| 3. 依赖 lock 重建慢/锁文件 EPERM | 本地 `npm install` 反复报 `EPERM`、10 分钟跑不完 | **能不引新依赖就不引**；HTML→Markdown 改为手写轻量转换器，避免改 `package-lock.json` |
| 4. Semi 2.101 无 Drawer | `import { Drawer }` 编译报错 | 所有侧边面板改用 `Modal` |
| 5. 主题主色被反引号污染 | `theme-index` 里主色写成 `` `#059669` emerald ``，UI 色块坏掉 | 解析时正则只取 hex 部分 |
| 6. `npm ci` 要求 lock 同步 | 手动改 `package.json` 后 CI 报 lock 不一致 | 改依赖必须同步更新 `package-lock.json`（本地 `npm install` 困难时，优先避免改依赖） |
| 7. 本地 `vite build` 删 dist 被沙箱拦截 | `EPERM: unlink dist/index.html` | 构建前先 `rm -rf dist`；或 `export NODE_OPTIONS='--use-system-ca'` 去掉 safe-delete 注入 |
| 8. git 提交报 COMMIT_EDITMSG 权限拒绝 | `fatal: could not open '.git/COMMIT_EDITMSG'` | 提交前 `rm -f .git/COMMIT_EDITMSG`；push 后若本地 ref 更新失败，`rm -f .git/logs/refs/remotes/origin/main` 再 `git fetch` |

---

## 七、目录结构（提交到仓库的部分）

```
gzh-design-web/
├─ index.html
├─ package.json            # 依赖 + scripts
├─ vite.config.ts
├─ tsconfig.json
├─ wrangler.toml          # pages_build_output_dir=dist, nodejs_compat
├─ LICENSE                # AGPL-3.0
├─ .github/workflows/deploy.yml   # 自动部署
├─ scripts/build-assets.mjs       # skill 资产 → skillAssets.ts
├─ functions/api/         # Pages Functions（部署时随项目根一起打包）
│   ├─ layout.ts
│   ├─ themes.ts
│   ├─ upload.ts
│   ├─ theme.ts
│   └─ generate.ts
├─ worker-lib/            # TS 版 skill 资产 + 移植脚本
│   ├─ skillAssets.ts     # 已提交，CI 不重新生成
│   ├─ themes.ts / validate.ts / extractDocx.ts / componentLint.ts / llm.ts
├─ src/                   # 前端
│   ├─ App.tsx            # 三栏编排
│   ├─ components/        # RichEditor / MarkdownEditor / ThemeBar / PreviewPanel / HistoryDrawer / SettingsDrawer / ModelManager / CustomThemeWizard / EditorToolbar
│   ├─ lib/               # api.ts / storage.ts / clipboard.ts / htmlToMarkdown.ts
│   ├─ config.ts          # REPO_URL（AGPL 源码链接）
│   └─ styles.css
└─ .workbuddy/            # WorkBuddy 内部记忆，已加进 .gitignore，不进公开仓
```

> `.gitignore` 必须忽略 `.workbuddy/`，否则内部记忆会进公开仓库。

---

## 八、部署流程（GitHub Actions，凭证只进 Actions secrets）

1. 本地 `gh` 已登录，直接 `gh repo create gzh-design-web --public --source . --remote origin --push`。
2. 用户在 GitHub 网页：**Settings → Secrets and variables → Actions → Repository secrets** 加两条：
   - `CLOUDFLARE_API_TOKEN`：Cloudflare 控制台新建，权限勾 **Cloudflare Pages: Edit** + 账号读。
   - `CLOUDFLARE_ACCOUNT_ID`：控制台地址栏 `dash.cloudflare.com/<这串>/...`。
3. 之后每次 `git push origin main` 自动：构建 → 建项目（若需）→ 部署。
4. 首次若报 project not found，见踩坑 #1（工作流已内置自动建项目）。

**优势**：开发者（我）只需要 GitHub 写权限，Cloudflare token 从不经过手，安全且省事。

---

## 九、复用此范本的 Checklist

新项目若要套用本模式，按此清单走：

- [ ] 明确「本地 skill/脚本 → 网页工具」的边界，列出 P0 功能清单
- [ ] 选 UI 库（Semi 则注意版本是否有 Drawer 等组件差异）
- [ ] 决定是否 BYOK + localStorage（小白用户优先）
- [ ] 把 skill 资产/脚本打包进仓库（Python→TS 移植），CI 不重新生成
- [ ] 设计 Serverless 代理接口（避开 CORS、保护 Key）
- [ ] 确定许可（SaaS/公开服务 → AGPL-3.0，仓库公开 + 页脚源码链接）
- [ ] 写 GitHub Actions 部署工作流，凭证进 Actions secrets
- [ ] 本地能跑通后，先建 GitHub 仓库并 push，再让用户填 secrets
- [ ] 验证：CI build 通过 + 线上 API 路由 + 首页可达
- [ ] 记录踩坑到项目记忆，方便下次复用

---

## 十、本次交付状态（截至 2026-07-30）

- 线上地址：https://gzh-design-web.pages.dev/ （部署成功，三栏布局可用）
- 已验证：主题列表、排版、自定义主题、图床代理、AI 文案生成接口均正常
- 仓库分支：`main`（所有功能均已合入并部署）
